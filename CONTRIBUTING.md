# Contributing

## Standards

- Keep rooms inspectable (human-readable manifests + append-only logs).
- Keep adapters provider-agnostic.
- Prefer deterministic behavior in core logic.
- Treat failures as indexable signals.

## Local dev

```bash
npm i
npm run dev -- --help
npm test
```
